import React from 'react';

// Display a Page Not Found message when user's url doesn't match a route
const NotFound = () => (
  <div>
    <h2>Page Not Found</h2>
  </div>
);

export default NotFound;